#!/bin/sh
# Query the generated reverse-engineering index.
set -eu

ROOT="$(CDPATH='' cd -- "$(dirname "$0")/.." && pwd)"
. "$ROOT/bin/python-env"
python_env "${PSX_PYTHON:-$ROOT/.venv/bin/python}" 2 'run `just venv` first'
exec "$PYTHON" -m harness.commands.rev_query "$@"
