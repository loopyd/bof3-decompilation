#!/bin/sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
exec "${SCRIPT_DIR}/bof3" toolchain psyq setup --dest "${DEST:-$(CDPATH= cd -- "${SCRIPT_DIR}/.." && pwd)/toolchains/psyq-original/4.0}" "$@"
