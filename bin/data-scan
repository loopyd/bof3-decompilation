#!/bin/sh
set -eu

ROOT="$(CDPATH='' cd -- "$(dirname "$0")/.." && pwd)"
. "$ROOT/bin/python-env"
python_env "${PSX_PYTHON:-$ROOT/.venv/bin/python}" 2 ''
exec "$PYTHON" -m harness.commands.data_scan "$@"
