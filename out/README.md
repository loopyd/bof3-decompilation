# out

Generated artifacts live here.

Current active outputs include:

- `out/ghidra-bootstrap/`
- `out/emi_raw/`
- `out/inventory/`
- `out/harness/`

The old `tmp/` and `processed/` paths are compatibility symlinks into this tree.
