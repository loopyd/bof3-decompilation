#!/bin/sh
# Wrapper for mipsel-none-elf-as that strips -c flag (ambiguous in GNU binutils 2.40+)
set -eu
DIR="$(cd "$(dirname "$0")" && pwd)"
ARGS=()
for arg in "$@"; do
    if [ "$arg" = "-c" ]; then
        continue
    fi
    ARGS+=("$arg")
done
exec "${DIR}/mipsel-none-elf-as" "${ARGS[@]}"
