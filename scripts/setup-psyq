#!/bin/sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
ROOT_DIR="$(CDPATH= cd -- "${SCRIPT_DIR}/.." && pwd)"

exec "${SCRIPT_DIR}/_bof3" toolchain psyq setup \
  --dest "${DEST:-${ROOT_DIR}/toolchains/psyq/4.7}" \
  "$@"
