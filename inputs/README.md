# inputs

Local supplied assets live here.

Use this tree for:

- disc images under `inputs/disc/`
- proprietary or local-only SDK archives under `inputs/external/`

This directory is user-owned rather than repo-owned.
