# inputs

Local runtime inputs live here.

Use this tree for:

- disc images under `inputs/disc/`
- local source archives under `inputs/external/` when you are staging from a local file

This directory is user-owned rather than repo-owned.
The optional `external/private-assets/` tree is separate and only used as a private import workspace.
